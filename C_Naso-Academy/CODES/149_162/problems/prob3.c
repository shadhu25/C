/*    GATE CS 2000
    the following C declarations

    struct node
    {
        int i;
        float j;
    };
    struct node *S[10];


    define (S) to be:

    a) an array, each element of which is a pointer to a structure of nde.
    b) a structure ps 2 fields, each field being a pointer to an array of
    10 elements.
    c) a structure of 3 fields: an integer, a float, and an array of 10
    elements.
    d) an array, each element of which is a structure of type node.


    answer: a)
*/